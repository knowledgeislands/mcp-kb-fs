# Knowledge Base Vault Configuration

# Absolute path or "~/..." to the vault root directory.
# The server validates all tool paths against this root and rejects any traversal outside it.
ROOT_PATH=~/your-vault
